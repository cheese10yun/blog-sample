spring:
  profiles:
    active: local


# Local
---
spring:
  profiles: local
  jpa:
    database: h2
    hibernate:
      ddl-auto: create-drop
    show-sql: true
    properties:
      hibernate.format_sql: true

logging:
  level:
    org:
      hibernate:
        type: trace