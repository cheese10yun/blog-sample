server:
    port: 8888

spring:
    application:
        name: config-server
    cloud:
        config:
            server:
                encrypt.enabled: true
                git:
                    uri: https://github.com/cheese10yun/msa-study-sample
                    search-paths:
                        - "licensingservice"